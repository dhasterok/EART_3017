# Minimal stub — no icon resources required for the gravity modelling GUI.
from pathlib import Path

ICONPATH = Path(__file__).parent.parent / "resources" / "icons" # benign default; ToggleSwitch does not use it
RESOURCE_PATH = Path(__file__).parent.parent / "resources"
