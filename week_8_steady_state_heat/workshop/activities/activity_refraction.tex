\begin{activity}{Thermal Refraction}

\section*{Purpose}
To build intuition for how low-conductivity surface layers redistribute temperature gradients without changing the total heat flow.

\section*{Learning Outcomes}

By the end of this activity, you should be able to:
\begin{itemize}
  \item Apply Fourier's law to explain why the temperature gradient differs between regions with constant heat flow.
  \item Sketch temperature--depth profiles for a layered crust and identify where isotherms are compressed or stretched.
  \item Predict which crustal section reaches higher interface temperatures and explain why.
  \item Discuss how varying conductivity affects geothermal energy potential.
\end{itemize}

\section*{Geological Context}
Consider two continental regions with identical basal heat flow (Figure~\ref{fig:ivs}):
\begin{description}
  \item Region A: crystalline crust exposed at the surface.
  \item Region B: crystalline crust overlain by a thick sedimentary basin.
\end{description}
\begin{figure}[h]
  \centering
  \includegraphics[]{figures/ig_vs_sed.pdf}
  \caption{Two continental regions: (a) igneous crust, (b) sedimentary basin over igneous crust. Both regions have the same basal heat flow.}
  \label{fig:ivs}
\end{figure}

The sediments are dominated by shales and porous clastic rocks with lower thermal conductivity than basement rock.  We will make the following assumptions:
\begin{itemize}
  \item no internal heat production,
  \item steady-state heat flow, and
  \item heat flow is vertical and laterally uniform.
\end{itemize}

\section*{Tasks}
\begin{questions}
  \question{Reasoning}
  What is the relationship between the surface heat flow of regions A and B?  What principle did you use to make this determination?
  \answerbox[2cm]

  Using Fourier's law,
  \[
    q = -k \frac{dT}{dz},
  \]
  explain qualitatively how the temperature gradient differs between the sedimentary basin and the igneous basement?
  \answerbox[3cm]

  \question{Sketch}
  Sketch temperature versus depth for both regions on the same axes.  Clearly label:
  \begin{itemize}
    \item sediment thickness,
    \item basement,
    \item relative steepness of gradients.
  \end{itemize}

  \begin{center}
  \begin{tikzpicture}
    % Dimensions (3:5 aspect ratio)
    \def\W{5}
    \def\H{7}

    % Axes
    \draw[->, thick] (0,0) -- (\W,0) node[right] {$T$};
    \draw[->, thick] (0,0) -- (0,-\H) node[below] {$z$};
  \end{tikzpicture}
  \end{center}

  \question{Explanation \& Discussion}

  \begin{enumerate}[label=\roman*]
    \item Which region would exhibit a higher temperature at the sediment--basement interface? Explain why.
    \answerbox[3cm]

    \item How does the presence of sediments affect measured geothermal gradients?
    \answerbox[2cm]

    \item Which region would you expect to have higher geothermal potential? Why?
    \answerbox[2cm]
  \end{enumerate}

  \question{Extension}
  If these two regions were adjacent to each other, how would heat flow and temperature vary in 2D?

  Start by drawing the heat flow lines, then the isotherms and finally the variation in surface heat flow on the axes provided.

  \medskip

  \begin{center}
  \begin{tikzpicture}[scale=0.85]

    \def\zb{-2}; % bottom of layer
    \def\base{-5} % base of model
    \def\w{6} % width
    \def\alen{0.5} % arrow length

    \pgfmathsetmacro{\z}{\base - \alen};

    % axes
    \draw[thin,->] (-\w,1) -- (-\w,5) node[above] {$q(x)$};
    \node[left] at (-\w,3) {$q_b$};
    \draw[thin,->] (-\w,3) -- (\w,3) node[left] {$x$};
    \draw[dashed,thin,gray!50] (0,1) -- (0,5);


    % Basin
    \path[fill=gray!30,draw=none]
        (0,0) -- (\w,0) -- (\w,\zb) -- (1,\zb) -- cycle;

    \draw[thin] (0,0) -- (1,\zb) -- (\w,\zb);

    % Surface
    \draw[thick] (-\w,0) -- (\w,0);

    % Base
    \draw[thick] (-\w,\base) -- (\w,\base);
    \foreach \x in {-5,-3,-1,1,3,5}
      \draw[->] (\x,\z) -- (\x,\base);

    % Label
    \node[below] at (-3,\z) {$q_b$};
    \node[below] at (3,\z) {$q_b$};
  \end{tikzpicture}
  \end{center}

\end{questions}

\section*{Key Ideas}

\textbf{A conductivity contrast redistributes the gradient, not the flux.} Whenever $q$ must stay constant across a boundary but $k$ changes, $dT/dz=-q/k$ forces the gradient to change instead. This effect is thermal refraction, and it applies at any conductivity contrast, not only a sediment-over-basement boundary -- the same reasoning will reappear for layered media, tilted strata, and mixed lithologies.

\textbf{A conductivity contrast redistributes the gradient, not the flux.} Whenever $q$ must stay constant across a boundary but $k$ changes, $dT/dz=-q/k$ forces the gradient to change instead. This is thermal refraction, and it applies at any conductivity contrast, not only a sediment-over-basement boundary -- the same reasoning will reappear for layered media, tilted strata, and mixed lithologies.

\textbf{A measured gradient reflects local conductivity, not the deeper structure.} The same heat flow can correspond to very different gradients depending on what you're measuring through, so gradients from different settings are not directly comparable without calculation of heat flow.  A gradient measured near the surface is only converted to a true heat flow once you know the conductivity at the depth it was measured. 

\end{activity}
